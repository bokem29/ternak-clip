import Landing from "./Landing";

const Index = () => {
  // Always show the landing page
  // Users will be redirected to role selection only when they try to access protected routes
  return <Landing />;
};

export default Index;
